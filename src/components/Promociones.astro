---
import { Image } from 'astro:assets';
import aire1 from '../img/aire1.webp';
import aire2 from '../img/aire2.webp';
---

<section id="promociones" class="py-20" style="overscroll-behavior-y: contain;">
    <div class="container mx-auto px-4">
        <h2 class="text-4xl font-bold text-center text-white mb-16">
            Promociones Especiales
        </h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <!-- Promoción 1 -->
            <div class="promo-card group" style="will-change: transform; transform: translateZ(0); content-visibility: auto; contain-intrinsic-size: 0 500px;">
                <div class="absolute top-4 right-4 bg-red-500 text-white px-4 py-1 rounded-full text-sm font-semibold">
                    -20%
                </div>
                <div class="flex flex-col md:flex-row gap-6">
                    <div class="w-full md:w-1/2 aspect-square bg-[#1B365D]/50 rounded-lg flex items-center justify-center">
                        <Image 
            src={aire1}
            alt="Unidad externa de aire acondicionado." 
            class="w-full h-full object-cover rounded-lg shadow-2xl relative z-0"
            width="512"
            height="512"
            loading="lazy"
            decoding="async"
            />
                    </div>
                    <div class="w-full md:w-1/2">
                        <h3 class="text-2xl font-semibold text-[#4994d2] mb-4">Mantenimiento Preventivo</h3>
                        <p class="text-gray-300 mb-4">Agenda tu mantenimiento anual con 20% de descuento. Incluye limpieza profunda y revisión completa.</p>
                        <div class="mb-4">
                            <span class="text-gray-400 line-through">$xxx,xxx</span>
                            <span class="text-white font-bold text-2xl ml-2">$xxx,xxx</span>
                        </div>
                        <button class="w-full px-6 py-3 bg-[#4994d2] text-white rounded-lg hover:bg-[#1B365D] transition-colors">
                            Aprovechar Oferta
                        </button>
                    </div>
                </div>
            </div>

            <!-- Promoción 2 -->
            <div class="promo-card group" style="will-change: transform; transform: translateZ(0); content-visibility: auto; contain-intrinsic-size: 0 500px;">
                <div class="absolute top-4 right-4 bg-red-500 text-white px-4 py-1 rounded-full text-sm font-semibold">
                    -15%
                </div>
                <div class="flex flex-col md:flex-row gap-6">
                    <div class="w-full md:w-1/2 aspect-square bg-[#1B365D]/50 rounded-lg flex items-center justify-center">
                        <Image 
            src={aire2}
            alt="Unidad externa de aire acondicionado." 
            class="w-full h-full object-cover rounded-lg shadow-2xl relative z-0"
            width="512"
            height="512"
            loading="lazy"
            decoding="async"
            />
                    </div>
                    <div class="w-full md:w-1/2">
                        <h3 class="text-2xl font-semibold text-[#4994d2] mb-4">Instalación + Mantenimiento</h3>
                        <p class="text-gray-300 mb-4">Paquete completo de instalación más primer mantenimiento gratis.</p>
                        <div class="mb-4">
                            <span class="text-gray-400 line-through">$xxx,xxx</span>
                            <span class="text-white font-bold text-2xl ml-2">$xxx,xxx</span>
                        </div>
                        <button class="w-full px-6 py-3 bg-[#4994d2] text-white rounded-lg hover:bg-[#1B365D] transition-colors">
                            Aprovechar Oferta
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Contador Regresivo -->
        <div class="mt-16 text-center">
            <p class="text-gray-300 mb-4">Estas ofertas terminan en:</p>
            <div class="flex justify-center gap-4">
                <div class="countdown-item" style="will-change: transform; transform: translateZ(0);">
                    <span class="number">24</span>
                    <span class="label">Días</span>
                </div>
                <div class="countdown-item" style="will-change: transform; transform: translateZ(0);">
                    <span class="number">12</span>
                    <span class="label">Horas</span>
                </div>
                <div class="countdown-item" style="will-change: transform; transform: translateZ(0);">
                    <span class="number">45</span>
                    <span class="label">Minutos</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .promo-card {
        position: relative;
        padding: 2rem;
        border-radius: 1rem;
        background: rgba(27, 54, 93, 0.3);
        backdrop-filter: blur(8px);
        border: 1px solid rgba(73, 148, 210, 0.2);
        will-change: transform;
        transform: translateZ(0);
        content-visibility: auto;
        contain-intrinsic-size: 0 500px;
    }

    .promo-card:hover {
        transform: translateY(-4px);
        background: rgba(27, 54, 93, 0.4);
        border-color: rgba(73, 148, 210, 0.3);
    }

    .countdown-item {
        padding: 1rem;
        min-width: 100px;
        border-radius: 0.5rem;
        background: rgba(27, 54, 93, 0.4);
        border: 1px solid rgba(73, 148, 210, 0.2);
        display: flex;
        flex-direction: column;
        align-items: center;
        will-change: transform;
        transform: translateZ(0);
    }

    .countdown-item .number {
        font-size: 2rem;
        font-weight: bold;
        color: #4994d2;
    }

    .countdown-item .label {
        color: #9CA3AF;
        font-size: 0.875rem;
    }

    @media (prefers-reduced-motion: reduce) {
        .promo-card {
            transition: none !important;
        }
    }
</style>
